{
  "name": "pictionary-word-generator",
  "version": "1.0.0",
  "description": "A static website to generate French pictionary words",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "http-server . -p 8080",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.5",
    "http-server": "^14.1.1"
  }
}

